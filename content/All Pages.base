filters:
  and:
    - file.ext == "md"
views:
  - type: table
    name: Table
    groupBy:
      property: file.folder
      direction: ASC
    order:
      - file.name
      - status
    sort:
      - property: file.name
        direction: ASC
